# Nome do Workflow
name: Deploy Códice to GitHub Pages

on:
  push:
    branches:
      - main
  # Permite rodar manualmente
  workflow_dispatch:

permissions:
  contents: write # Necessário para o peaceiris/actions-gh-pages

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          # O caminho para o cache deve ser relativo à raiz do repositório
          cache-dependency-path: story-view/package-lock.json

      - name: Install dependencies
        run: npm install
        # Define o diretório de trabalho apenas para este passo
        working-directory: ./story-view

      - name: Build project
        # CORREÇÃO AQUI: O caminho para o script de build foi corrigido.
        run: node build/build.js
        # O diretório de trabalho continua sendo ./story-view, então o caminho relativo está correto.
        working-directory: ./story-view

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          # Publica a pasta story-view inteira
          publish_dir: ./story-view
